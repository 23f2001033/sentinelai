# Pinned to the versions this project is actually tested against.
# Playwright must stay in step with the browser image in Dockerfile.
fastapi==0.141.1
uvicorn[standard]==0.52.0
sqlalchemy==2.0.51
aiosqlite==0.22.1
pydantic==2.13.4
pydantic-settings==2.14.2
openai==2.51.0
anthropic==0.120.2
playwright==1.61.0
pyyaml==6.0.3
python-multipart==0.0.32
httpx==0.28.1
pytest==9.1.1
pytest-asyncio==1.4.0

# Only needed if DATABASE_URL points at Postgres instead of SQLite:
# asyncpg==0.30.0
